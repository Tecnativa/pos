<?xml version="1.0" encoding="UTF-8"?>

<templates id="template" xml:space="preserve">

<t t-extend="Product">
    <t t-jquery="t[t-if='!product.to_weight']" t-operation="replace">
        <t t-if="!product.to_weight">
            <span class="price-tag">
                <t t-if="widget.pos.config.iface_tax_included">
                    <t t-esc="widget.format_currency(widget.pos.get_product_price(product), 'Product Price')"/>
                </t>
                <t t-if="!widget.pos.config.iface_tax_included">
                    <t t-esc="widget.format_currency(product.price,'Product Price')"/>
                </t>
            </span>
        </t>
        <t t-if="product.to_weight">
            <t t-if="widget.pos.config.iface_tax_included">
                <t t-esc="widget.format_currency(widget.pos.get_product_price(product), 'Product Price')+'/Kg'"/>
            </t>
            <t t-if="!widget.pos.config.iface_tax_included">
                <t t-esc="widget.format_currency(product.price,'Product Price')+'/Kg'"/>
            </t>
        </t>
    </t>
</t>

<t t-extend="PosTicket">
    <t t-jquery="t[t-esc='orderline.get_quantity_str_with_unit()']" t-operation="after">
        <t t-if="orderline.quantity != 1">
            x<t t-esc="widget.format_currency_no_symbol(orderline.get_unit_display_price(), 'Product Price')"/>
        </t>
    </t>
</t>

</templates>
